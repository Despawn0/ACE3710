MEMORY {
    CODE: start = $0, size = $20, type = ro;
    DATA: start = $20, size = $10, type = rw;
    BSS: start = $10, size = $10, type = bss;
}

SEGMENTS {
    CODE: load = CODE, fill = yes;
    DATA: load = DATA, fill = yes;
}